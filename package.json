{
  "name": "assignment-vacuum-cleaner",
  "private": true,
  "scripts": {
    "build": "nitro build",
    "dev": "nitro dev",
    "prepare": "nitro prepare",
    "preview": "node .output/server/index.mjs",
    "lint": "eslint . && prettier -c .",
    "lint:fix": "eslint . --fix && prettier -w .",
    "test": "nr lint && vitest run --coverage"
  },
  "devDependencies": {
    "@types/node": "^22.8.1",
    "@vitest/coverage-v8": "2.1.3",
    "eslint": "^9.13.0",
    "eslint-config-unjs": "^0.4.1",
    "nitropack": "latest",
    "prettier": "^3.3.3",
    "typescript": "^5.6.3",
    "vitest": "^2.1.3"
  },
  "dependencies": {
    "pg": "^8.13.0",
    "zod": "^3.23.8"
  }
}
